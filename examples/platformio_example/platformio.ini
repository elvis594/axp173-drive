[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200

; 如果库已发布到PlatformIO Registry
; lib_deps = AXP173-Driver

; 如果使用本地库
lib_deps = 
    ; 其他依赖库可以在这里添加

; 如果从GitHub直接引用
; lib_deps = https://github.com/your-username/axp173-drive.git

[env:esp32s3]
platform = espressif32
board = esp32-s3-devkitc-1
framework = arduino
monitor_speed = 115200
lib_deps = 
    ; 同上